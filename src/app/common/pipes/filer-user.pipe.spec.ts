import { FilerUserPipe } from './filer-user.pipe';

describe('FilerUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FilerUserPipe();
    expect(pipe).toBeTruthy();
  });
});
